<UserControl x:Class="CustomStrokeForge.Views.StrokeEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:CustomStrokeForge.ViewModels"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             mc:Ignorable="d" Name="this"
             Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
             Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
             d:DataContext="{d:DesignInstance Type=vm:StrokeEditorViewModel}">
    <UserControl.Resources>
        <Style TargetType="ToggleButton" x:Key="TabBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                BorderThickness="1" CornerRadius="3" Margin="0,0,0,4" Padding="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Custom Stroke Forge"
                           FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"
                           VerticalAlignment="Center" Margin="4,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ToggleButton Style="{StaticResource TabBtn}"
                                  Content="{x:Static local:Texts.TabStroke}"
                                  IsChecked="{Binding IsStrokeTabActive, Mode=OneWay}"
                                  Click="TabStroke_Click"
                                  xmlns:local="clr-namespace:CustomStrokeForge"/>
                    <ToggleButton Style="{StaticResource TabBtn}"
                                  Content="{x:Static local:Texts.TabPattern}"
                                  IsChecked="{Binding IsPatternTabActive, Mode=OneWay}"
                                  Click="TabPattern_Click"
                                  xmlns:local="clr-namespace:CustomStrokeForge"/>
                    <ToggleButton Style="{StaticResource TabBtn}"
                                  Content="{x:Static local:Texts.TabAdvanced}"
                                  IsChecked="{Binding IsAdvancedTabActive, Mode=OneWay}"
                                  Click="TabAdvanced_Click"
                                  xmlns:local="clr-namespace:CustomStrokeForge"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <c:PropertiesEditor Target="{Binding SelectedTarget}"
                                BeginEdit="PropertiesEditor_BeginEdit"
                                EndEdit="PropertiesEditor_EndEdit"/>
        </ScrollViewer>
    </Grid>
</UserControl>
